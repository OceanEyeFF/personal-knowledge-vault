# 项目进度体检（2026-03-12）

> 结论口径：基于仓库现有“事实基线 / 路线图 / Changelog / 最近提交”进行静态评估。

## 一、总体结论（TL;DR）

- 项目整体已从“检索型知识库”推进到“关系推理能力起步”阶段。
- 按当前路线（Phase 0 → A → B → C → D）评估：
  - **Phase 0：已完成（100%）**
  - **Phase A：已完成并收尾（≈95%-100%）**
  - **Phase B：已启动但仍属 bootstrap（≈30%-40%）**
  - **Phase C：未正式展开（0%-10%，仅有测试基础设施）**
  - **Phase D：未正式展开（0%-10%，GUI 保持工作台定位）**
- 综合加权后，当前项目完成度可暂估为：**约 60%-70%（面向“当前路线总目标”）**。

## 二、评估依据

1. `docs/overview/当前事实基线-2026-03.md`
2. `docs/overview/阶段开发路线与依赖-2026-03.md`
3. `docs/operations/CHANGELOG.md`
4. `README.md`
5. `git log` 最近提交

## 三、分阶段进度判断

### Phase 0（Baseline Alignment）

- 事实基线、路线、差异清单均已建立，并在文档中明确为后续统一真相源。
- 判断：**完成**。

### Phase A（Relation Foundation）

- 路线文档明确 `Batch 1~3` 已完成、`Batch 4/T6` 收尾。
- 关系模型、关系存储、迁移、抽取、回填、一跳查询、最小验证闭环均已落地。
- 判断：**完成（收尾态）**。

### Phase B（推理型 MCP）

- 已有 `query_subgraph`、`explain_relation`、`collect_evidence`、`find_bridges/timeline_of/contrast(partial)`。
- 文档同时明确该批能力属于“最小起步 / partial implementation”，并非完整推理能力闭环。
- 判断：**已启动，处于 bootstrap 阶段**。

### Phase C（评测与验证）

- 已有单元/集成/E2E/黑盒测试基础，但缺少“面向推理型 MCP 的黑盒评测闭环”正式完成证据。
- 判断：**基础具备，专项闭环未完成**。

### Phase D（GUI 工作台适配）

- GUI 多视图已存在，但当前路线中定位是“浏览与调试工作台”，非主战场。
- 判断：**能力可用，但不作为当前里程碑重点推进项**。

## 四、当前主要风险 / 卡点

1. 推理能力仍偏“弱语义 + 启发式”，高阶结果可解释性和稳定性有限。
2. relation backfill 仍以安全路径为主，真实库正式回填与验证尚未形成标准化节奏。
3. `collect_evidence` 仍是文档级证据包 v1，尚未到 chunk 级证据管理。
4. 缺少面向推理型 MCP 的可持续评测基准（数据集 + 指标 + 回归门禁）。

## 五、下一步优先建议（按 ROI 排序）

1. **先做 Phase C 最小闭环**：定义 10~20 条代表性推理任务，固化输入/期望输出/评分规则。
2. **补强 Phase B 三个 partial 工具**：为 `find_bridges/timeline_of/contrast` 增加更明确的数据约束和置信度字段。
3. **推进真实库回填演练**：在测试副本库执行 `--apply` 回填并产出质量报告（覆盖率、噪声率、冲突率）。
4. **将“完成定义”前置**：给 Phase B/C 分别设可量化 Exit Criteria（例如成功率、解释可读性、误报率）。

## 六、建议的项目状态标签

- 当前状态建议标注为：**`Phase A closeout complete / Phase B bootstrap in progress`**。
- 对外描述建议：**“具备检索 + 最小关系推理能力，正在进入评测驱动的能力增强阶段”**。

