# R4-E：恢复矩阵、文档与源码验收记录（2026-09）

> 状态：**source acceptance complete**（2026-09-03）。
>
> 实现基线：`907c576`（`feat(r4): add durable ingestion lifecycle and blackbox acceptance`）。
> 本记录只证明源码、受控测试运行器和合成数据上的行为；不构成 Artifact、安装器、PyPI、真实
> Provider、真实 Vault、真实迁移或 release acceptance。当前 headless candidate 的合规 hold 不变。

## 1. 范围与结论

R4 将归档实现为持久但非 daemon 的内部生命周期：

```text
公开 CLI / MCP stdio archive
  -> Q0 admission + private PreparedDocument
  -> Q1′ Markdown + SQLite core commit + durable handoff
  -> Q2 policy / reservation / Provider / DerivationPatch
  -> Q1′ patch commit + staged generation / validate / pointer CAS
  -> READY generation-backed vector / hybrid retrieval
```

当前 Application 在一次允许的 mutation 中有界 drain 上述任务；进程退出后没有独立后台
worker。只读 CLI/MCP、status、search 与 related 不取得 lease、不 drain、不创建 Provider，
也不写 data root。

公开接口没有新增 rebuild API、CLI rebuild 命令或 MCP rebuild Tool。R4 的 source acceptance
证明既有 `archive*`、`show/get_entry` 和 vector/hybrid search 能正确投影该内部生命周期，
而不是扩大产品发布面。

## 2. R4-E 退出条件与证据

| 原退出条件 | 覆盖和可复核证据 | 结果 |
| --- | --- | --- |
| 隔离运行器、进程外 deterministic Provider harness、Windows P0 与 MCP gate | 所有命令经 `scripts/run-test.ps1`；每个测试使用独立 `.data-test/<scenario>`；`tests/offline_entrypoint.py` 在导入产品前清除 live 配置/代理并安装网络 guard。`test-conda.ps1 -Suite P0` 与 `-Suite MCP` 均经该入口运行。 | 已完成 |
| snapshot/reload 与 lease 竞争 | `test_knowledge_application.py`、`test_runtime_lifecycle.py`、`test_r4_ingress_lifecycle.py` 与 `test_r4_derivation_lifecycle.py` 覆盖 immutable snapshot、reload、fenced claim、busy 零副作用和 expiry recovery。Provider barrier 还实际探测 writer lease 可取得。 | 已完成 |
| Q0 / Q1′ / Q2 restart 与 handoff crash | Q0 cancellation/reload、Q1′ `core_committed` / `handoff_recorded` / `binding_published` / `q2_activated` 故障点、Q2 expired claim/new fence/retry/not-before 都有单元生命周期回归。 | 已完成 |
| stale plan、Q1′ patch failure、generation pre/post-CAS | source drift 与 late-provider fence、patch core-commit 后故障且不重调 LLM、`pre_cas` / `post_cas` 参数化 generation recovery 均有回归。 | 已完成 |
| budget pause 与授权后 resume | Provider 创建前的 token/price cap 阻断、零 reservation/patch，以及重新确认 policy 后以新 Config 完成同一 operation 均有回归。 | 已完成 |
| 真实公开 CLI 跨进程闭环 | `tests/blackbox/test_r4_cli_fullflow.py`：真实 `archive-text` 子进程完成 Q0/Q1′/Q2、结算和 READY；全新 CLI `show`、vector 与 hybrid search 命中同一 entry。 | 已完成 |
| 真实 MCP stdio 跨进程闭环 | `tests/blackbox/test_r4_mcp_fullflow.py`：MCP SDK/JSON-RPC 经 `offline_entrypoint.py mcp` 调用真实 `archive_text`；关闭 server 后新 server 的 `get_entry`、vector 与 hybrid search 命中同一 entry。 | 已完成 |
| durable ledger、usage 与 generation | `tests/blackbox/r4_fullflow_support.py` 按公开 `operation_id` / `knowledge_id` 精确关联 Q0、Q1′、handoff、Q2、reservation、usage、Markdown/SQLite revision、generation manifest/binding；不以时间或随机 UUID 排序猜测任务。 | 已完成 |
| 文档和价格叙述收敛 | 本记录及 README、一级事实文档、Runtime/Config/Application/Workflow/AI/Storage/CLI/MCP 模块指南同步 R4 生命周期；没有已确认 price card 时只记录 token，不估算或显示货币金额。 | 已完成 |
| 发布边界 | 本记录、测试说明与变更日志均明确 source acceptance 不提升 Artifact/release 状态。 | 已完成 |

## 3. 外置 deterministic Provider harness

`tests/harness/r4_openai_compatible_provider.py` 是测试拥有的独立进程，不是产品代码、
Artifact payload 或隐藏 test mode。它只实现正式 OpenAI-compatible 配置可调用的非流式
chat completion 与 embedding 端点，并以固定模型、维度、向量、usage 和版本化步骤脚本给出
确定性响应。

- harness 只监听随机 `127.0.0.1` numeric-loopback 端口；端口、DataRoot、配置和 state dir
  每个场景独立。
- 测试运行器只为该精确 loopback endpoint 放行 Provider 使用的 resolver/connection helper；
  DNS、hostname 和所有非 loopback 网络仍 fail-closed。产品 SSRF/SafeFetcher 策略没有放宽。
- barrier 以事件/有界 timeout 协调 Provider 调用并证明 writer lease 未被持有；测试不依赖
  `sleep` 作为正确性同步。
- telemetry 只保存 endpoint、阶段、计数与摘要 hash，不保存 prompt/body、密钥或 Authorization。

## 4. 运行记录

下列记录针对 `907c576` 所含 R4 实现；全部只使用合成 `.data-test` 根，未选择 `network` 或
`manual`，没有读取用户 Vault、用户配置或真实 Provider 凭据。

| 命令类别 | 结果 |
| --- | --- |
| R4 lifecycle 定向回归（Q0/Q1′/Q2、Application、Runtime） | `103 passed in 174.45s` |
| CLI + MCP R4 进程边界黑盒 | `2 passed in 47.62s`；另一次独立重复运行也通过，确认无端口/顺序依赖。 |
| `./scripts/test-conda.ps1 -EnvironmentName py311-private -Suite P0` | `3274 passed, 21 skipped, 219 deselected, 35 warnings in 1740.61s`，exit 0。 |
| `./scripts/test-conda.ps1 -EnvironmentName py311-private -Suite MCP` | `779 passed, 5 deselected in 466.75s`，`src.mcp 95.15%`，exit 0。 |
| `python -m evals.mcp_quality --enforce-thresholds`（经 `run-test.ps1 -Direct`） | `thresholds_met: true`，exit 0。 |
| 致命 Ruff 静态检查（`E9,F63,F7,F82`）与 `git diff --check` | 全部通过。全仓 `ruff format --check` 仍有 210 个既有文件会被重排，属于独立格式化债务，不是本轮 source acceptance 的致命检查。 |

## 5. 已知后续边界

| 场景 | 当前结论 | 原因和后续条件 |
| --- | --- | --- |
| MCP `archive_url` 成功的离线黑盒 | 不作为 R4 source acceptance 成功路径 | SafeFetcher 正确拒绝 loopback；当前没有既不削弱 SSRF 又能公开注入外置 fetch harness 的合同。DNS/redirect 的 fail-closed、零写测试保留。 |
| 失败或预算暂停后，仅靠新 CLI/MCP 只读入口恢复同一 task | 后续 public-resume 设计项 | 读入口按 R4 合同不得 drain；当前只能由下一次允许 mutation/内部 trigger 恢复，不能伪装成公开 resume。 |
| Provider 调用中强杀进程后的即时重领 | 后续测试基础设施项 | 没有公开测试时钟、durable provider-start 标记或缩短 claim 的产品合同；不得为测试加入后门或削弱 fence。 |

这些限制不影响已覆盖的 Q0/Q1′/Q2 deterministic-harness recovery matrix，也不应被写成 release blocker 的
替代描述。R3.1 writer ownership、任何 public rebuild adapter、Node、Docker、GUI 和真实数据
验证继续按各自工作包与授权门禁推进。

## 6. 关联文档

- [R4 自动 AI 生命周期与费用控制实施计划](./R4-自动AI生命周期与费用控制实施计划-2026-08.md)
- [知识变更与 Embedding Generation 一致性 ADR](./ADR-知识变更与EmbeddingGeneration一致性-2026-08.md)
- [Runtime 生命周期合同](../specs/interfaces/Runtime生命周期合同.md)
- [Tests 测试模块说明](../../tests/CLAUDE.md)
