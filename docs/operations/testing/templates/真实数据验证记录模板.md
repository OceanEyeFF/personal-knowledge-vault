# 真实数据验证空白记录模板（T-A … T-H）

> 所属流程：[真实数据验证 Runbook](../真实数据验证Runbook.md)
> 状态：**空白模板**（本文件不含任何真实数据；全部字段为占位符）
> 使用规则：执行 P0/P1/P2 时复制对应模板到 `.data-test/<scenario>/evidence/` 填写；填写后的文件不得提交进 Git。

---

## T-A 授权单（Authorization Record）

> 对应 Runbook 4.1；没有签核的授权单，任何命令不得执行。

```text
授权单 ID:            AUTH-<阶段>-<yyyyMMdd>-<序号>
阶段:                 P0 / P1 / P2
签发时间:             <yyyy-MM-dd HH:mm>
授权令牌:             <随机令牌 ID，非签名/姓名>
数据所有者确认:        <确认内容与时间>

样本清单 ID:          SMPL-<阶段>-<yyyyMMdd>-<序号>
快照路径:             .data-test/<scenario>/
数据分级:             [ ] D1 公开  [ ] D2 内部  [ ] D3 个人（须逐条列明）
                       D4 敏感：禁止，未选择

允许的命令类别:        [ ] CAT-0 Agent-safe 静态/合成（Agent 可执行）
                       [ ] CAT-U 用户手动·快照命令（离线与 live，含 archive/vector/hybrid）
                       [ ] CAT-C 迁移演练（仅 clones/<clone-id> 内）
                       CAT-D 生产触碰：永不授权
执行通道说明:           真实快照命令一律由用户手动执行（8.1）；Agent 只接收脱敏摘要（12 章）
live/数据出境命令范围与预算上限: [ ] 无（默认）  [ ] 有：<命令清单>/<预算上限>
                       说明：PKV_RUN_LIVE 仅是测试收集开关，不是网络开关（7.3）

留存期限:             <天数> 天（快照与报告）
清除方式:             <删除 .data-test/<scenario> 目录 / 移动归档>
撤销条件:             用户随时可撤销；发现 D4 或任何泄露自动失效

授权人确认:            <授权令牌 + 时间戳>（用户口头/书面确认，不保存签名或姓名）
```

---

## T-B 样本清单（Sample Manifest）

> 对应 Runbook 5.3；每条真实样本以**假名/占位标识**登记，不写原文。

```yaml
sample_manifest: pkv.real_sample_manifest.v1
sample_id: SMPL-<阶段>-<yyyyMMdd>-<序号>
auth_id: AUTH-<阶段>-<yyyyMMdd>-<序号>
data_class: [D1]            # 实际分级；禁止 D4
pseudonym_map_ref: <映射文件路径，仅存 .data-test，不入 Git>
strata:
  content_type: {wechat: 0, zhihu: 0, chat: 0, generic: 0, text_fallback: 0}
  source_domains: 0
  time_ranges: 0
  sizes: {short: 0, long: 0, code: 0, table: 0}
  languages: [zh, en]
negative_cases: [empty_body, long_title, missing_time_field]
size_limit: {entries: <N>, bytes_mb: <M>}
items:
  - ref_id: SMPL-P0-REF-01        # 假名条目标识（禁止原始 URL/正文/姓名）
    content_type: <wechat|zhihu|chat|generic|text_fallback>
    source_domain: <占位域或 text>
    archived_time: <占位时间段>
    size_class: <short|long|code|table>
    language: <zh|en>
    negative: <none|empty_body|long_title|missing_time_field>
    pseudonym: PERSON-<NN>       # 需要假名化时的标识
  # 以下按实际样本数复制占位行；禁止填写原始 URL、正文或姓名（Runbook 5.3）
representativeness_statement: >-
  本样本覆盖 <分层维度>，共 <N> 条，按 <选择规则> 抽取。
  只代表所覆盖的分层；指标仅作真实数据质量观测值，不作为发布结论。
```

---

## T-C 人工判读表（Interpretation Sheet）

> 对应 Runbook 10.2；三态结论：通过 / 不通过 / 需复查。判读依据必须引用证据位置。

| 行号 | 样本 ref_id | 判读维度 | 期望结果（占位） | 实际结果摘要（只写假名/ID） | 结论 | 判读依据（证据文件+位置） | 备注/后续任务 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | SMPL-P0-REF-01 | archive 正确性 | 标题/正文/来源/时间与样本一致 | <占位> | <通过/不通过/需复查> | <占位> | <占位> |
| 2 | SMPL-P0-REF-01 | search 相关性@k | top-k 命中查询意图 | <占位> | <占位> | <占位> | <占位> |
| 3 | SMPL-P0-REF-02 | evidence 可解析 | 每条 citation locator 可打开且一致 | <占位> | <占位> | <占位> | <占位> |
| 4 | SMPL-P0-REF-02 | delete 完整性 | 删除后无残留/无孤儿 | <占位> | <占位> | <占位> | <占位> |
| 5 | SMPL-P0-REF-03 | migration 数据完整 | 迁移前后计数与字段一致 | <占位> | <占位> | <占位> | <占位> |
| … | … | … | … | … | … | … | … |

判读规则（Runbook 10.2）：

- `需复查` = 边界模糊 / 脱敏存疑 / 需用户凭假名映射回查原文。
- 表中只出现假名与样本 ID；出现任何真实内容即视为违规并触发 F5。
- **证据最小化**：实际结果摘要只允许人工脱敏摘要、计数、哈希与假名 ID；不保存原始 stdout/stderr、URL、正文或姓名（Runbook 10.2/12 章）。

---

## T-D 自动门禁结果（Gate Record）

> 对应 Runbook 10.3。**证据契约**：本表只记录**脱敏摘要、退出码、计数、哈希、时间与模板指纹**；
> **禁止原样记录命令输出、真实命令实参、URL、正文、姓名、绝对路径或密钥**（Runbook 12 章）。

| 门禁 | 机制/类别（占位） | 脱敏摘要 | 退出码 | 判定 | 时间 | 模板指纹/备注 |
| --- | --- | --- | --- | --- | --- | --- |
| G0 base-only 受控入口 | `offline_entrypoint.py`（或 S7 等价）；只证明入口本身 | <占位> | <0/非0> | <通过/失败> | <时间> | **硬前置**；未就绪不可执行；不保护未接入入口的命令 |
| G1 离线套件 | 合成 pytest（CAT-0） | <占位> | <0/非0> | <通过/失败> | <时间> | <占位> |
| G2 MCP 16-task | 合成 `OfflineMcpScenario`（CAT-0） | <占位> | <0/非0> | <通过/失败> | <时间> | <占位> |
| G3 快照一致性 | `stats` 用户执行 + manifest 核对（摘要） | <占位> | <0/非0> | <通过/失败> | <时间> | 仅计数/哈希 |
| G4 隔离核验 | base-only 预检（仅用户或 S7），**不用 `config show`** | <占位> | <0/非0> | <通过/失败> | <时间> | <占位> |
| G5 脱敏扫描 | <S1 工具，占位> | <占位> | <0/非0> | <通过/失败> | <时间> | 含证据最小化检查 |
| G6 指标漂移 | P2 趋势对比（默认指标） | <占位> | <0/非0> | <通过/失败> | <时间> | <占位> |
| G7 真实快照 MCP harness | <S6 工具，待实现> | — | — | — | — | **未交付前不可执行**，从默认门禁剔除 |

---

## T-E 失败分流记录（Failure Triage Record）

> 对应 Runbook 第 11 章。

| 事件 ID | 时间 | 阶段 | 失败现象（只写字段/假名） | 分类 | 暂停? | 决策 | 后续任务 | 关闭时间 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TRG-<日期>-<序号> | <时间> | P0/P1/P2 | <占位> | F1/F2/F3/F4/F5 | <是/否> | <占位> | <占位> | <时间> |
| … | … | … | … | … | … | … | … | … |

安全事件（F5 / D4）附加要求：事件记录中只写字段名与假名，不写泄露原文；处理按 Runbook 11.3。

---

## T-F 审计证据清单（Audit Evidence Checklist）

> 对应 Runbook 第 12 章；证据存放 `.data-test/<scenario>/evidence/`。

| 证据 | 来源 | 路径（占位） | 完整性检查 | 已归档 |
| --- | --- | --- | --- | --- |
| E1 授权单 | 用户签发 | `<占位>` | ID 与样本清单对应；无签名/姓名 | [ ] |
| E2 样本清单 | 用户/执行者协作 | `<占位>` | manifest 有效；仅假名/占位 ID | [ ] |
| E3 命令日志 | 用户执行，Agent 整理 | `<占位>` | 仅命令类别 + 模板指纹 + 退出码 + 哈希 + 时间 + 脱敏摘要；**无真实命令/实参/URL/正文** | [ ] |
| E4 判读表 | 判读人 | `<占位>` | 逐条有结论；脱敏摘要/计数/假名 | [ ] |
| E5 门禁记录 | 执行者 | `<占位>` | 退出码齐备（已实现项，T-D 契约） | [ ] |
| E6 指标记录 | 执行者 | `<占位>` | 默认指标有口径；剔除/未覆盖项有声明 | [ ] |
| E7 分流记录 | 全体 | `<占位>` | 分类与决策齐备 | [ ] |
| E8 清除凭证 | 用户 | `<占位>` | 清除时间、退出码、哈希 | [ ] |
| E9 原始证据（可选） | **仅用户** | `<工作区之外、用户 ACL 隔离、Agent 不可读>` | 只存位置指针与哈希，不存内容（不得用 `.data-backup/` 或工作区内目录） | [ ] |

---

## T-G 留存/清除记录（Retention & Cleanup Record）

> 对应 Runbook 第 13 章；清除只能由用户执行。

| 对象 | 路径（占位） | 留存期限 | 清除时间 | 清除退出码/哈希摘要 | 确认（Test-Path 结果） | 凭证归档 |
| --- | --- | --- | --- | --- | --- | --- |
| 授权快照 | `.data-test/<scenario>/` | <天> | <时间> | <占位> | <不存在=已清除> | [ ] |
| 假名映射 | `<占位>` | <天> | <时间> | <占位> | <不存在=已清除> | [ ] |
| disposable clone | `.data-test/<scenario>/clones/<clone-id>/` | ≤7 天 | <时间> | <占位> | <不存在=已清除> | [ ] |
| 报告与证据包 | `<占位>` | <天> | <时间> | <占位> | <占位> | [ ] |
| 原始证据（可选） | `<工作区之外、用户 ACL 隔离、Agent 不可读>` | 用户自定 | <时间> | <占位> | <占位> | [ ]（E9） |
| 执行日志 | `<占位>` | <天> | <时间> | <占位> | <占位> | [ ] |

---

## T-H 阶段执行记录（Phase Run Record）

> 对应 Runbook 第 9 章；每个阶段一份。

```text
阶段:                 P0 / P1 / P2
场景名:               .data-test/<scenario>/
执行时间:             <yyyy-MM-dd HH:mm – yyyy-MM-dd HH:mm>
授权单:               <AUTH-ID>（授权令牌）
样本清单:             <SMPL-ID>
排练基底（OR）:        (#3 dev vault 已就绪) / (合成样本预演降级)（18.2）
执行通道记录:          CAT-0（Agent 执行项清单）/ CAT-U 与 CAT-C（用户手动执行项与脱敏摘要 ID）
隔离核验结果:          <G0 base-only 受控入口输出摘要，不含 local.yaml>（7.1-5/10.3-G0）
clone 记录:           clone-id / 制备时间 / 源快照哈希（仅用户，7.1-7/15.5）
历史 schema baseline: 版本号 + 哈希（用户准备，15.5）
pending migration:    待迁移脚本数（dry-run 摘要）；pending=0 时 migration 指标标"未覆盖"
快照条目数:            <N>（应等于 manifest 申报数）
门禁结果:              G0..G7 已实现项汇总（T-D，脱敏摘要契约）
指标记录（默认 + 剔除/未覆盖声明）:
  search 相关性@k（BM25）: <数值>
  migration 兼容性:       <数值或"未覆盖"（pending=0 时）>
  只读一致性:             <数值>
  剔除项声明:             [archive/live 向量/MCP evidence/citation/delete]
                        未纳入原因: <live 未授权 / S3 未交付 / S6 未交付>（10.1）
判读结论:              <T-C 汇总：通过/不通过/需复查 计数>（脱敏摘要/计数）
分流事件:              <T-E 汇总>
阶段结论:              <通过 / 差距报告（引用 TRG-ID）>
DoD 核对:              <逐条勾选 Runbook 9.x 对应 DoD>
清除记录:              <T-G ID>
```

---

## 模板索引（对应 Runbook 章节）

| 模板 | 内容 | 对应章节 |
| --- | --- | --- |
| T-A | 授权单 | 4.1 |
| T-B | 样本清单 manifest | 5.3 |
| T-C | 人工判读表 | 10.2 |
| T-D | 自动门禁结果 | 10.3 |
| T-E | 失败分流记录 | 11 |
| T-F | 审计证据清单 | 12 |
| T-G | 留存/清除记录 | 13 |
| T-H | 阶段执行记录 | 9 |

**模板版本**: v1.2（2026-07-31 复审修订：双通道执行模型与证据契约统一）
**创建日期**: 2026-07-31
