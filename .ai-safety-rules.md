# AI 协作安全规则

> **重要**: 此文件定义了 AI 助手（如 Claude Code）在协作时必须遵守的安全规则 ฅ'ω'ฅ

---

## 核心原则

### 🔒 生产数据保护

AI 助手在协作时：

1. **禁止直接操作生产数据**
   - ❌ 不得读取 `.data/` 目录下的文件
   - ❌ 不得执行会影响生产数据库的命令
   - ❌ 不得读取或修改本机敏感配置（`config/local.yaml`）

2. **强制使用测试环境**
   - ✅ 所有测试必须使用 `.\scripts\run-test.ps1`；CLI 子命令直接传入，pytest/Python 等命令必须使用 `-Direct -Command @(...)`
   - ✅ 非 CLI 测试可显式设置 `$env:DATA_DIR = ".data-test/<scenario>"`，其余路径会随之隔离
   - ✅ 在执行任何数据操作前，先验证环境隔离

3. **备份要求**
   - ✅ 建议用户在重要变更前执行 `.\scripts\backup-data.ps1`
   - ✅ 不主动执行备份（需用户确认）

---

## 安全检查清单

### 执行命令前检查

AI 助手在建议执行以下命令前，必须先提醒用户：

```powershell
# ❌ 危险：直接操作生产环境
python -m src.main archive "https://example.com"
python -m pytest tests/integration/  # 如果会写入生产数据库

# ✅ 安全：使用测试环境
.\scripts\run-test.ps1 archive "https://example.com"
.\scripts\run-test.ps1 -Direct -DataRoot .data-test/integration -Command @("pytest", "tests/integration/")
```

### 环境验证命令

在执行任何数据操作前，AI 应建议运行：

```powershell
# 通过测试包装脚本验证数据库路径；不得直接加载生产默认配置
.\scripts\run-test.ps1 config show

# 预期输出（测试环境）：
# DB Path: <仓库路径>\.data-test\db\knowledge_vault.db
```

---

## 典型场景处理

### 场景 1：用户要求"测试新功能"

**AI 响应模板**：

```
为了保护您的生产数据，浮浮酱建议在测试环境执行喵～

1. 在测试环境执行（脚本会自行建立完整路径隔离）：
   ```powershell
   .\scripts\run-test.ps1 <CLI-subcommand>
   .\scripts\run-test.ps1 -Direct -Command @("<executable>", "<arg1>", "<arg2>")  # pytest/Python 等
   ```

2. 验证命令仍指向测试环境，并检查代码工作树：
   ```powershell
   .\scripts\run-test.ps1 config show
   git status --short
   ```

   AI 不通过读取 `.data/` 来验证生产数据；如需额外确认，由用户自行检查备份或文件时间戳。
```

---

### 场景 2：用户要求"修改数据库 Schema"

**AI 响应模板**：

```
Schema 变更是高风险操作喵！浮浮酱建议按以下顺序执行：

1. 准备隔离测试数据：
   - 全新的 `.data-test\schema-migration` 空目录只验证 **fresh install**，不能证明旧库升级兼容，也不能作为生产升级门禁。
   - 如需验证升级兼容性，先把已脱敏的旧版本 DB/Schema 夹具放到 `.data-test\schema-migration\db\knowledge_vault.db`；场景依赖 Vault 或向量数据时，也应把对应测试夹具放入同一数据根目录。
   - 不要让测试命令直接指向生产 `.data/`。

2. 在同一测试数据根目录检查并执行迁移：
   ```powershell
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\schema-migration -Command @("python", "scripts\migrate.py", "--dry-run")
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\schema-migration -Command @("python", "scripts\migrate.py", "--auto", "--no-backup")
   ```

3. 在同一测试数据根目录验证数据完整性：
   ```powershell
   .\scripts\run-test.ps1 -DataRoot .data-test\schema-migration stats
   .\scripts\run-test.ps1 -DataRoot .data-test\schema-migration list
   ```

4. 测试成功后，如需生产迁移，必须先取得用户明确授权，并由用户备份生产数据：
   ```powershell
   .\scripts\backup-data.ps1 -Message "生产环境 Schema 升级前最终备份"
   ```

5. 生产迁移只由用户执行已在旧版测试夹具上验证过的命令。

6. AI 不主动读取 `.data/` 验证结果；由用户依据备份、迁移日志和应用健康检查完成生产验证。
```

---

### 场景 3：用户要求"运行集成测试"

**AI 检查点**：

集成测试可能会写入数据库，需要确认：

```powershell
# ✅ 安全：Direct 模式同样隔离全部运行时路径
.\scripts\run-test.ps1 -Direct -DataRoot .data-test/integration -Command @("pytest", "tests/integration/", "-v")
```

---

### 场景 4：用户要求"查看数据库内容"

**AI 响应**：

```powershell
# ✅ 默认只查看测试数据库
.\scripts\run-test.ps1 stats

# 如果需要查看测试条目：
.\scripts\run-test.ps1 list --limit 10

# ❌ AI 不读取或导出生产 `.data/` 内容
# 如需检查生产数据，必须由用户明确授权并自行确认输出范围
```

---

## 自动安全提示

### AI 在以下情况必须主动提示

1. **检测到生产环境命令**
   ```
   ⚠️ 检测到您正在使用生产环境命令！

   建议改用测试环境：
   .\scripts\run-test.ps1 <CLI-subcommand>
   .\scripts\run-test.ps1 -Direct -Command @("<executable>", "<arg1>", "<arg2>")

   或者先备份：
   .\scripts\backup-data.ps1
   ```

2. **检测到数据库写操作**
   ```
   ⚠️ 此操作将修改数据库！

   已验证环境隔离？
   [ ] 是的，我正在使用测试环境
   [ ] 否，我需要先切换到测试环境
   ```

3. **检测到 Schema 变更**
   ```
   ⚠️ Schema 变更检测！

   强烈建议：
   1. 先备份：.\scripts\backup-data.ps1
   2. 在测试环境验证
   3. 再应用到生产环境
   ```

---

## 禁止的操作

AI 助手**绝对不应该**建议或执行：

1. ❌ 直接删除 `.data/` 目录或其子目录
2. ❌ 执行 `DROP TABLE` 等破坏性 SQL 语句（除非在测试环境）
3. ❌ 读取、打印或修改 `config/local.yaml` 中的密钥
4. ❌ 在未备份的情况下执行 Schema 迁移
5. ❌ 批量删除数据（除非用户明确要求且已备份）
6. ❌ 覆盖已有的备份文件

---

## 推荐的工作流

### 开发新功能

```
1. 创建功能分支
   git checkout -b feature/new-feature

2. 在测试环境开发
   .\scripts\run-test.ps1 archive "test-url"

3. 运行测试
   .\scripts\run-test.ps1 -Direct -Command @("pytest", "tests/", "-v")

4. 验证仍使用测试数据路径，并检查代码工作树
   .\scripts\run-test.ps1 config show
   git status --short

5. 提交代码
   git add <本次明确修改的文件路径>
   git commit -m "feat: 添加新功能"
```

### 修复 Bug

```
1. 备份生产数据
   .\scripts\backup-data.ps1 -Message "修复 Bug 前"

2. 在测试环境复现问题（CLI）
   .\scripts\run-test.ps1 <CLI-subcommand>

3. 在测试环境验证修复（pytest/Python 等非 CLI）
   .\scripts\run-test.ps1 -Direct -Command @("<executable>", "<arg1>", "<arg2>")

4. 应用到生产环境（如果需要数据修复）
   # 谨慎操作，确认备份存在
```

---

## 环境变量检测脚本

`scripts/check-environment.ps1` 当前会通过 `run-test.ps1` 把配置检查和数据库统计固定到 `.data-test/check-environment`，同时隔离数据库、Vault、向量、日志和临时目录；它不会读取或写入生产 `.data/`。可直接用它验证隔离环境：

```powershell
.\scripts\check-environment.ps1
```

其他测试仍应通过 `run-test.ps1` 运行，并按场景显式指定独立的 `-DataRoot`。

---

## 总结

**AI 助手应该**：
- ✅ 默认推荐测试环境
- ✅ 主动提示备份
- ✅ 验证环境隔离
- ✅ 给出安全的命令示例

**AI 助手不应该**：
- ❌ 直接操作生产数据
- ❌ 执行破坏性操作（除非明确要求且已备份）
- ❌ 跳过安全检查

---

**文档版本**: v1.0
**作者**: 幽浮酱 ฅ'ω'ฅ
**最后更新**: 2026-02-16

*安全第一，数据无价喵～*
