# AI 协作安全规则

> **重要**: 此文件定义了 AI 助手（如 Claude Code）在协作时必须遵守的安全规则 ฅ'ω'ฅ

---

## 核心原则

### 🔒 生产数据保护

AI 助手在协作时：

1. **禁止直接操作生产数据**
   - ❌ 不得读取 `.data/` 目录下的文件
   - ❌ 不得执行会影响生产数据库的命令
   - ❌ 不得修改生产环境配置（`.env`）

2. **强制使用测试环境**
   - ✅ 所有测试必须使用 `.\scripts\run-test.ps1`
   - ✅ 或显式设置 `$env:DB_PATH = ".data-test/db/knowledge_vault.db"`
   - ✅ 在执行任何数据操作前，先验证环境隔离

3. **备份要求**
   - ✅ 建议用户在重要变更前执行 `.\scripts\backup-data.ps1`
   - ✅ 不主动执行备份（需用户确认）

---

## 安全检查清单

### 执行命令前检查

AI 助手在建议执行以下命令前，必须先提醒用户：

```powershell
# ❌ 危险：直接操作生产环境
python -m src.main archive "https://example.com"
python -m pytest tests/integration/  # 如果会写入生产数据库

# ✅ 安全：使用测试环境
.\scripts\run-test.ps1 archive "https://example.com"
$env:DB_PATH = ".data-test/db/knowledge_vault.db"; python -m pytest tests/integration/
```

### 环境验证命令

在执行任何数据操作前，AI 应建议运行：

```powershell
# 验证当前使用的数据库路径
python -c "from src.utils.config import Config; print(f'DB Path: {Config().db_path}')"

# 预期输出（测试环境）：
# DB Path: E:\gitee\personal-knowledge-vault\.data-test\db\knowledge_vault.db

# 预期输出（生产环境）：
# DB Path: E:\gitee\personal-knowledge-vault\.data\db\knowledge_vault.db
```

---

## 典型场景处理

### 场景 1：用户要求"测试新功能"

**AI 响应模板**：

```
为了保护您的生产数据，浮浮酱建议在测试环境执行喵～

1. 首先验证测试环境配置：
   ```powershell
   # 如果没有 .env.test，从模板创建
   if (!(Test-Path .env.test)) { copy .env.test.example .env.test }
   ```

2. 在测试环境执行：
   ```powershell
   .\scripts\run-test.ps1 <your-command>
   ```

3. 验证生产数据未受影响：
   ```powershell
   python -m src.main stats  # 应该显示原有数据量
   ```
```

---

### 场景 2：用户要求"修改数据库 Schema"

**AI 响应模板**：

```
Schema 变更是高风险操作喵！浮浮酱建议以下流程：

⚠️ **必须先备份**：
```powershell
.\scripts\backup-data.ps1 -Message "Schema 变更前备份"
```

然后按以下顺序执行：

1. 在测试环境验证迁移脚本：
   ```powershell
   $env:DB_PATH = ".data-test/db/knowledge_vault.db"
   python scripts/migrate_schema.py  # 您的迁移脚本
   ```

2. 验证测试环境数据完整性：
   ```powershell
   .\scripts\run-test.ps1 stats
   .\scripts\run-test.ps1 list
   ```

3. 如果测试成功，再次备份生产数据：
   ```powershell
   .\scripts\backup-data.ps1 -Message "生产环境 Schema 升级前最终备份"
   ```

4. 在生产环境执行迁移（谨慎！）

5. 验证生产数据：
   ```powershell
   python -m src.main stats
   python -m src.main list
   ```
```

---

### 场景 3：用户要求"运行集成测试"

**AI 检查点**：

集成测试可能会写入数据库，需要确认：

```powershell
# ✅ 安全：使用测试数据库运行
$env:DB_PATH = ".data-test/db/knowledge_vault.db"
python -m pytest tests/integration/ -v

# 或使用 fixtures 自动隔离（推荐）
python -m pytest tests/integration/ -v  # 如果 conftest.py 已配置隔离
```

---

### 场景 4：用户要求"查看数据库内容"

**AI 响应**：

```powershell
# ✅ 安全：仅读取统计信息（不显示敏感内容）
python -m src.main stats

# 如果需要查看具体条目：
python -m src.main list --limit 10

# ❌ 避免：导出完整数据库（可能泄露敏感信息）
# 如需导出，建议用户手动确认
```

---

## 自动安全提示

### AI 在以下情况必须主动提示

1. **检测到生产环境命令**
   ```
   ⚠️ 检测到您正在使用生产环境命令！

   建议改用测试环境：
   .\scripts\run-test.ps1 <command>

   或者先备份：
   .\scripts\backup-data.ps1
   ```

2. **检测到数据库写操作**
   ```
   ⚠️ 此操作将修改数据库！

   已验证环境隔离？
   [ ] 是的，我正在使用测试环境
   [ ] 否，我需要先切换到测试环境
   ```

3. **检测到 Schema 变更**
   ```
   ⚠️ Schema 变更检测！

   强烈建议：
   1. 先备份：.\scripts\backup-data.ps1
   2. 在测试环境验证
   3. 再应用到生产环境
   ```

---

## 禁止的操作

AI 助手**绝对不应该**建议或执行：

1. ❌ 直接删除 `.data/` 目录或其子目录
2. ❌ 执行 `DROP TABLE` 等破坏性 SQL 语句（除非在测试环境）
3. ❌ 修改 `.env` 文件的 API Keys
4. ❌ 在未备份的情况下执行 Schema 迁移
5. ❌ 批量删除数据（除非用户明确要求且已备份）
6. ❌ 覆盖已有的备份文件

---

## 推荐的工作流

### 开发新功能

```
1. 创建功能分支
   git checkout -b feature/new-feature

2. 在测试环境开发
   .\scripts\run-test.ps1 archive "test-url"

3. 运行测试
   python -m pytest tests/ -v

4. 验证生产环境未受影响
   python -m src.main stats

5. 提交代码
   git add .
   git commit -m "feat: 添加新功能"
```

### 修复 Bug

```
1. 备份生产数据
   .\scripts\backup-data.ps1 -Message "修复 Bug 前"

2. 在测试环境复现问题
   .\scripts\run-test.ps1 <reproduce-command>

3. 在测试环境验证修复
   .\scripts\run-test.ps1 <verify-command>

4. 应用到生产环境（如果需要数据修复）
   # 谨慎操作，确认备份存在
```

---

## 环境变量检测脚本

AI 可以建议用户运行此脚本来验证环境：

```powershell
# scripts/check-environment.ps1
Write-Host "========================================" -ForegroundColor Cyan
Write-Host " 环境检测" -ForegroundColor Green
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

# 检查 DB_PATH 环境变量
if ($env:DB_PATH) {
    Write-Host "当前数据库路径（环境变量）:" -ForegroundColor Yellow
    Write-Host "  $env:DB_PATH" -ForegroundColor White

    if ($env:DB_PATH -match "\.data-test") {
        Write-Host "  ✓ 测试环境" -ForegroundColor Green
    } else {
        Write-Host "  ⚠️  生产环境" -ForegroundColor Red
    }
} else {
    Write-Host "当前数据库路径（默认配置）:" -ForegroundColor Yellow
    $db_path = python -c "from src.utils.config import Config; print(Config().db_path)"
    Write-Host "  $db_path" -ForegroundColor White

    if ($db_path -match "\.data-test") {
        Write-Host "  ✓ 测试环境" -ForegroundColor Green
    } else {
        Write-Host "  ⚠️  生产环境" -ForegroundColor Red
    }
}

Write-Host ""
Write-Host "数据库统计:" -ForegroundColor Yellow
python -m src.main stats | Select-String "总条目数"
```

---

## 总结

**AI 助手应该**：
- ✅ 默认推荐测试环境
- ✅ 主动提示备份
- ✅ 验证环境隔离
- ✅ 给出安全的命令示例

**AI 助手不应该**：
- ❌ 直接操作生产数据
- ❌ 执行破坏性操作（除非明确要求且已备份）
- ❌ 跳过安全检查

---

**文档版本**: v1.0
**作者**: 幽浮酱 ฅ'ω'ฅ
**最后更新**: 2026-02-16

*安全第一，数据无价喵～*
