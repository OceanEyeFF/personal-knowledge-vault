# AI 协作安全规则

> **重要**: 此文件定义了 AI 助手（如 Claude Code）在协作时必须遵守的安全规则 ฅ'ω'ฅ

---

## 核心原则

### 当前路径合同

- 唯一可编辑的用户配置是 `%USERPROFILE%\.pkv\config.yaml`。AI 不读取、打印或修改它，其中可能包含 Provider Key/Cookie。
- 有效数据根按 `PKV_DATA_ROOT`（若设置）→ 用户配置的 `storage.data_root` → `%USERPROFILE%\.pkv\data` 解析；下文的 `<effective-data-root>` 均指这个最终单一根。
- `PKV_DATA_ROOT` 与 `PKV_LOG_LEVEL` 是正式产品环境覆盖；`DATA_DIR`、细粒度子路径变量与 `LOG_LEVEL` 仅在 `PKV_TEST_OFFLINE=1` 的测试包装器中兼容。
- `<effective-data-root>\config\local.yaml` 是 PKV 管理、无敏感字段的 runtime snapshot，不是第二份用户配置；AI 不手工编辑它，也不把它当作凭据来源。
- `.data-test/<scenario>/` 只用于受 `scripts/run-test.ps1` 约束的合成/离线测试。它绝不等同于用户数据根。
- checkout 中旧 `.data/`、旧 `config/local.yaml`，以及 held release candidate 的 `%LOCALAPPDATA%\PersonalKnowledgeVault` 仅是历史用户资产/证据。不得自动发现、接管、复制、迁移或删除；任何后续处理必须由用户走 inspect → plan → 展示影响与备份 → 确认 → execute → smoke test。

### 🔒 生产数据保护

AI 助手在协作时：

1. **禁止直接操作生产数据**
   - ❌ 不得读取 `<effective-data-root>/` 下的文件或从中导出内容
   - ❌ 不得执行会影响生产数据库的命令
   - ❌ 不得读取或修改本机敏感配置（`%USERPROFILE%\.pkv\config.yaml`），也不得把 runtime snapshot 当作可编辑配置

2. **强制使用测试环境**
   - ✅ 所有自动化必须使用 `.\scripts\run-test.ps1`，并为每个场景显式指定隔离的 `-DataRoot .data-test\<scenario>`；CLI 子命令直接传入，pytest/受保护的 Direct Python 必须使用 `-Direct -Command @(...)`
   - ✅ pytest 由包装器路由到 `tests/offline_entrypoint.py pytest`，在 pytest/plugin 导入前建立 G0；根 `tests/conftest.py` 再维持逐用例隔离，禁止参数绕过该边界
   - ✅ CLI/MCP 离线子进程由 `tests/offline_entrypoint.py` 启动
   - ✅ Direct Python（FT7）不是通用仓库脚本运行器：只接受 pytest；模块 `src.cli.commands`、`src.mcp.server`、`src.utils.verify_setup`、`evals.mcp_quality`；以及脚本 `scripts/setup-test-db.py`、`scripts/rebuild-dev-vault.py`、`scripts/check_chunk_index_consistency.py`、`src/utils/verify_setup.py`。其余仓库模块/脚本会在创建 DataRoot 前拒绝；完整参数与路径规则以 `scripts/CLAUDE.md` 为准。`-c`、stdin 和解释器 flags 同样拒绝，并在同一解释器中经 `runpy` 执行
   - ✅ Direct Python 在导入产品代码前清理 live/secret/proxy 环境，安装 base-only Config、网络 guard 与子进程 guard；这些是 Python 进程内保护，不是 OS sandbox
   - ⚠️ 非 Python `-Direct` 仍须经 wrapper 启动，但不属于 Python G0、不保证离线；必须另做边界和副作用审查
   - ✅ `scripts/setup-test-db.py` 的输出必须精确位于本次所选 `DATA_DIR`
   - ✅ 在执行任何数据操作前，先验证环境隔离

3. **备份要求**
   - ✅ 建议用户在重要变更前执行 `.\scripts\backup-data.ps1`
   - ✅ 不主动执行备份（需用户确认）

---

## 安全检查清单

### 执行命令前检查

AI 助手在建议执行以下命令前，必须先提醒用户：

```powershell
# ❌ 危险：直接操作生产环境
python -m src.main archive "https://example.com"
python -m pytest tests/integration/  # 如果会写入生产数据库

# ✅ 安全：使用测试环境
.\scripts\run-test.ps1 -DataRoot .data-test\safety-command stats
.\scripts\run-test.ps1 -Direct -DataRoot .data-test/integration -Command @("python", "-m", "pytest", "tests/integration/")
```

### 环境验证命令

在执行任何数据操作前，AI 应建议运行：

```powershell
# 通过测试包装脚本验证数据库路径；不得直接加载生产默认配置
.\scripts\run-test.ps1 -DataRoot .data-test\safety-config config show

# 预期输出（测试环境）：
# DB Path: <仓库路径>\.data-test\db\knowledge_vault.db
```

---

## 典型场景处理

### 场景 1：用户要求"测试新功能"

**AI 响应模板**：

```
为了保护您的生产数据，浮浮酱建议在测试环境执行喵～

1. 在测试环境执行（脚本会自行建立完整路径隔离）：
   ```powershell
   .\scripts\run-test.ps1 -DataRoot .data-test\<scenario> <CLI-subcommand>
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\<scenario> -Command @("python", "-m", "src.cli.commands", "--help")
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\<scenario> -Command @("python", "src\utils\verify_setup.py")
   # 其他 Direct Python target 必须逐项属于本文件上方的 allowlist；不可用通配占位符代替。
   ```

2. 验证命令仍指向测试环境，并检查代码工作树：
   ```powershell
   .\scripts\run-test.ps1 -DataRoot .data-test\<scenario> config show
   git status --short
   ```

   AI 不通过读取 `<effective-data-root>/` 来验证生产数据；如需额外确认，由用户自行检查备份或文件时间戳。
```

---

### 场景 2：用户要求"修改数据库 Schema"

**AI 响应模板**：

```
Schema 变更是高风险操作喵！浮浮酱建议按以下顺序执行：

1. 默认自动化只运行 pytest 创建并回收的临时 SQLite 迁移测试。空库只能验证 **fresh install/迁移代码契约**，不能证明旧库升级兼容，也不能作为生产升级门禁。真实或历史快照不得放入 CAT-0；旧库兼容演练必须等待 U1/G8/FT5，并由用户在 disposable writable clone 中执行。

2. 当前不要通过测试包装器执行 `migrate.py`：`run-test.ps1` 会 fail-closed 并以 exit 2 拒绝，因为迁移入口尚未接入等价的 base-only 安全边界。

3. 仅需合成开发库时，通过受保护的 Direct Python 运行重建和只读复核。脚本要求 FT7 runtime attestation，并把 `--root` 锁在本次所选 `DATA_DIR` 内；裸启动会在产品 import 前失败：
   ```powershell
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\rebuild-dev -Command @("python", "scripts\rebuild-dev-vault.py", "--root", ".data-test/rebuild-dev", "--json")
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\rebuild-dev -Command @("python", "scripts\rebuild-dev-vault.py", "--root", ".data-test/rebuild-dev", "--check-only", "--json")
   ```

4. 真实旧库/生产迁移仍受 **U1/G8/FT5 user-only gate** 阻塞；截至当前尚未执行任何真实数据迁移。门禁交付后仍必须先取得用户明确授权，并由用户备份生产数据：
   ```powershell
   .\scripts\backup-data.ps1 -Message "生产环境 Schema 升级前最终备份"
   ```

5. 门禁全部交付后，生产迁移也只能由用户执行已在获批 disposable clone 上验证过的命令；AI/自动化到此停止。

6. AI 不主动读取 `<effective-data-root>/` 验证结果；由用户依据备份、迁移日志和应用健康检查完成生产验证。
```

---

### 场景 3：用户要求"运行集成测试"

**AI 检查点**：

集成测试可能会写入数据库，需要确认：

```powershell
# ✅ 安全：Direct 模式同样隔离全部运行时路径
.\scripts\run-test.ps1 -Direct -DataRoot .data-test/integration -Command @("python", "-m", "pytest", "tests/integration/", "-v")
```

---

### 场景 4：用户要求"查看数据库内容"

**AI 响应**：

```powershell
# ✅ 默认只查看测试数据库
.\scripts\run-test.ps1 -DataRoot .data-test\safety-read stats

# 如果需要查看测试条目：
.\scripts\run-test.ps1 -DataRoot .data-test\safety-read list --limit 10

# ❌ AI 不读取或导出生产 <effective-data-root> 内容
# 如需检查生产数据，必须由用户明确授权并自行确认输出范围
```

---

## 自动安全提示

### AI 在以下情况必须主动提示

1. **检测到生产环境命令**
   ```
   ⚠️ 检测到您正在使用生产环境命令！

   建议改用测试环境：
   .\scripts\run-test.ps1 -DataRoot .data-test\<scenario> <CLI-subcommand>
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\<scenario> -Command @("python", "-m", "src.cli.commands", "--help")

   或者先备份：
   .\scripts\backup-data.ps1
   ```

2. **检测到数据库写操作**
   ```
   ⚠️ 此操作将修改数据库！

   已验证环境隔离？
   [ ] 是的，我正在使用测试环境
   [ ] 否，我需要先切换到测试环境
   ```

3. **检测到 Schema 变更**
   ```
   ⚠️ Schema 变更检测！

   强烈建议：
   1. 先备份：.\scripts\backup-data.ps1
   2. 在测试环境验证
   3. 再应用到生产环境
   ```

---

## 禁止的操作

AI 助手**绝对不应该**建议或执行：

1. ❌ 直接删除 `<effective-data-root>/` 目录或其子目录
2. ❌ 执行 `DROP TABLE` 等破坏性 SQL 语句（除非在测试环境）
3. ❌ 读取、打印或修改 `%USERPROFILE%\.pkv\config.yaml` 中的密钥，或手工改写 `<effective-data-root>\config\local.yaml`
4. ❌ 在未备份的情况下执行 Schema 迁移
5. ❌ 批量删除数据（除非用户明确要求且已备份）
6. ❌ 覆盖已有的备份文件

---

## 推荐的工作流

### 开发新功能

```
1. 创建功能分支
   git checkout -b feature/new-feature

2. 在测试环境开发
   .\scripts\run-test.ps1 -DataRoot .data-test\feature-search search "synthetic query" --strategy bm25

3. 运行测试
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\feature-pytest -Command @("python", "-m", "pytest", "tests/", "-v")

4. 验证仍使用测试数据路径，并检查代码工作树
   .\scripts\run-test.ps1 -DataRoot .data-test\feature-config config show
   git status --short

5. 提交代码
   git add <本次明确修改的文件路径>
   git commit -m "feat: 添加新功能"
```

### 修复 Bug

```
1. 备份生产数据
   .\scripts\backup-data.ps1 -Message "修复 Bug 前"

2. 在测试环境复现问题（CLI）
   .\scripts\run-test.ps1 -DataRoot .data-test\<scenario> <CLI-subcommand>

3. 在测试环境验证修复（pytest 或受保护的、显式 allowlisted Direct Python）
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\bug-pytest -Command @("python", "-m", "pytest", "tests/", "-v")
   .\scripts\run-test.ps1 -Direct -DataRoot .data-test\bug-verify -Command @("python", "src\utils\verify_setup.py")

4. 应用到生产环境（如果需要数据修复）
   # 谨慎操作，确认备份存在
```

---

## 环境变量检测脚本

`scripts/check-environment.ps1` 当前会通过 `run-test.ps1` 把配置检查和数据库统计固定到 `.data-test/check-environment`，同时隔离数据库、Vault、向量、日志和临时目录；它不会读取或写入生产 `<effective-data-root>/`。可直接用它验证隔离环境：

```powershell
.\scripts\check-environment.ps1
```

其他测试仍应通过 `run-test.ps1` 运行，并按场景显式指定独立的 `-DataRoot`。

---

## 总结

**AI 助手应该**：
- ✅ 默认推荐测试环境
- ✅ 主动提示备份
- ✅ 验证环境隔离
- ✅ 给出安全的命令示例

**AI 助手不应该**：
- ❌ 直接操作生产数据
- ❌ 执行破坏性操作（除非明确要求且已备份）
- ❌ 跳过安全检查

---

**文档版本**: v1.2
**作者**: 幽浮酱 ฅ'ω'ฅ
**最后更新**: 2026-08-21

*安全第一，数据无价喵～*
